<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.free_grind" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Shown by the system (via androidx.core.splashscreen) from process
         start until MainActivity explicitly dismisses it once the WebView
         has actually painted content — see MainActivity.onCreate's
         installSplashScreen()/setKeepOnScreenCondition and JsBridge's
         notifyContentReady(). Without this, the system splash disappears
         as soon as the window itself is drawn (near-instant), leaving a
         blank white/black WebView background visible for the several
         seconds it then takes to load and render the app on top of it. -->
    <style name="Theme.free_grind.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.free_grind</item>
    </style>
</resources>
